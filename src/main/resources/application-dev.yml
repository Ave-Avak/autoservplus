spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
  thymeleaf:
    cache: false
  devtools:
    restart:
      enabled: true

logging:
  level:
    be.autoservplus: DEBUG
    org.hibernate.SQL: DEBUG
